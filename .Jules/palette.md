## 2024-05-15 - Focus-visible and ARIA Labels\n**Learning:** This app's interactive elements (inputs and buttons) lacked explicit `focus-visible` states, and password fields lacked `aria-label` attributes.\n**Action:** Ensure all interactive elements have `:focus-visible` styles applied for keyboard navigation and ensure all inputs have corresponding accessible labels (`aria-label` or `<label>`).
